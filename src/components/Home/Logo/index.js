import { useEffect, useRef } from 'react'
import './index.scss'

const Logo = () => {
  const bgRef = useRef()


 

  return (
    <div className="logo-container" ref={bgRef}>
     
   
    </div>
  )
}

export default Logo
